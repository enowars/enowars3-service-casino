version: '2.3'
services:
  casino-backend:
    build: .
  casino-frontend:
    image: nginx:1.13-alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - casino-backend
    ports:
      - 5001:80
